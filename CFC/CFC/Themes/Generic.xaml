<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CFC.Views">


    <Style TargetType="{x:Type local:ListBoxItemEX}">
        <Setter Property="Height" Value="15" />
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ListBoxItemEX}">
                    <Grid>
                        <TextBlock Text="{TemplateBinding ItemName}" />
                        <TextBlock Text="{TemplateBinding ItemPrice}" HorizontalAlignment="Right" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
